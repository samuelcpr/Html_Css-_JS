// Manipulando Strings e Números

// Contar quantos caracteres tem uma palavra e quantos
// digitos tem um número

let word = "Paralelepipedo"
// length mostra quantos caracteres tem o word
console.log(word.length) //vai ser imprimido 14

/* essa funcionalidade e para caracteres porem podemos usar
a conversão com o metodo (string(number).length) e converter 
o numero em string e ver a quantidade de caractere que ele tem.
*/
let number = 1234
console.log(String(number).length)
